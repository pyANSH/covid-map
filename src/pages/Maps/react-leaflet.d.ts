import { TileLayerProps, MapContainer } from "react-leaflet";

export declare module "react-leaflet" {
  interface TileLayerProps {
    attribution?: string;
  }
  interface leaflet {
    Icon?: any;
  }
}
